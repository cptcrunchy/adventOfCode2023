import { getPuzzle } from './readFile'
import arr from '@arrows/array'
import com from '@arrows/composition'
import dis from '@arrows/dispatch'
import * as R from 'ramda'
import * as iter from 'iter-tools'
import * as graph from 'graphlib'

export { getPuzzle, arr, com, dis, iter, R, graph }
export default { getPuzzle, R, iter }